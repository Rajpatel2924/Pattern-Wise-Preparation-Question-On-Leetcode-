# Trie

Trie (prefix tree) stores strings/prefixes for fast prefix queries.

Common scenarios:
- Word dictionaries / prefix search
- Auto-complete
- XOR queries with binary trie

Checklist:
- Node children mapping (array/map)
- Mark end-of-word
- Traverse prefixes carefully

