# Breadth-First Search (BFS)

Explore nodes level-by-level using a queue.

Common scenarios:
- Shortest path in unweighted graphs
- Level order traversal of trees
- Grid problems with distance (cells visited in layers)

Checklist:
- Use visited set/marking to avoid cycles
- Queue holds (node, distance) when needed
- For grids: handle boundaries + directions

