# Heap (Priority Queue)

Use a heap to repeatedly get min/max efficiently.

Common scenarios:
- Top-K elements
- Merging sorted lists
- Scheduling / running median

Checklist:
- Choose min-heap vs max-heap strategy
- Store (key, metadata) pairs when needed
- Complexity: push/pop is O(log n)

